[package]
name = "live2d-render"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
live2d-core.workspace = true
live2d-probe = { workspace = true, optional = true }

[features]
default = []
probe = ["dep:live2d-probe"]
